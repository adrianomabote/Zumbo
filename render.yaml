services:
  - type: web
    name: zumbopay-bridge
    runtime: node
    buildCommand: echo "sem dependencias"
    startCommand: node zumbopay-bridge.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
